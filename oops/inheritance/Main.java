package inheritance;

public class Main {
   public static void main(String[] args) {
   //Box box1=new Boxweight();
   //box1.example();
    //System.out.println(box1.l+" "+box1.h+" "+box1.w+" "+box1.weight);

//Boxweight box2=new Boxweight(5, 3, 2, 10);
   //System.out.println(box2.l+" "+box2.h+" "+box2.w+" "+box2.weight);

  // Box box3=new Boxweight(5, 3, 2, 10);
   //System.out.println(box3.l+" "+box3.h+" "+box3.w+" ");//+box3.weight);//here,we cannot access weight.
   //this is because the reference type of box3 is box and box has no idea about the variable weight
   //weight is a variable of boxweight class
   //so,the variables which we can access depends on the type of reference  

   //Boxweight box4=new Box(1, 2, 3);
   //this is not allowed in java
   //explanation in notes

   //Boxprice box4=new Boxprice(5, 6, 7, 8, 9);
   //System.out.println(box4.l+" "+box4.h+" "+box4.w+" "+box4.weight+" "+box4.price);

   } 
}

